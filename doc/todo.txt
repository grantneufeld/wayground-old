For a list of to-do’s (and other action notes) directly from the code, enter:
 rake notes
from the project root.


Split out a separate path controller from the items_controller.
 Action: show. Displays the related Item, Event or whatever.
 Action: index. Lists paths and links to their objects (edit links for admins).
